version: "3.9"

services:
  postgres:
    image: postgres:16
    container_name: postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: appuser
      POSTGRES_PASSWORD: dUbocica#1389!!!
      POSTGRES_DB: appdb
    volumes:
      - pgdata:/var/lib/postgresql/data
    networks:
      - web
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U appuser -d appdb || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
    # nema port mappinga → DB nije izložen internetu

  pgadmin:
    image: dpage/pgadmin4:8
    container_name: pgadmin
    restart: unless-stopped
    environment:
        PGADMIN_DEFAULT_EMAIL: lazar.vujovic1993@gmail.com
      PGADMIN_DEFAULT_PASSWORD: dUbocica#1389!!!
    ports:
      - "127.0.0.1:5050:80"  # samo lokalno
    networks:
      - web
    depends_on:
      - postgres

volumes:
  pgdata:

networks:
  web:
    external: true
